{
  "name": "Sales-Inventory",
  "version": "1.0.0",
  "dependencies": {
    "@cap-js/hana": "^2",
    "@sap/approuter": "^23.0.0",
    "@sap/cds": "^9",
    "express": "^4",
    "node-cron": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2",
    "@sap/cds-dk": "^9"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "cds": {
    "server": {
      "index": true
    },
    "requires": {
      "bpaIntegration": {
        "kind": "rest",
        "credentials": {
          "destination": "bpaIntegration"
        }
      },
      "[production]": {
        "auth": "dummy"
      }
    }
  }
}
